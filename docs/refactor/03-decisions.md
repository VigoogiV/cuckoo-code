# Cuckoo Code 重构决策记录

> 状态：草稿 v0.1
> 用途：记录影响架构的关键决策及其理由、日期、影响面。
> 格式：每条含「决策 / 背景 / 备选 / 结论 / 影响」。

---

## D1. TS 迁移与重构的先后顺序

- **背景**：目标是「模块化重构 + 迁移 TS」。两者都是大工程。
- **备选**：
  - (a) 先重构，后 TS
  - (b) 先 TS，后重构
  - (c) 同时进行
- **结论**：**(b) 先 TS（渐进），后重构**。顺序：P1 清死代码 → P2 TS 基建 → P3 渐进迁移 → P4 重构 → P5 收紧。
- **理由**：
  1. TS 是重构的安全网 —— 改接口时编译器 catch 所有调用点。
  2. 迁移是机械的（行为不变），重构是创造性的（故意改结构），必须分开做。
  3. 先删死代码（P1）再迁 TS，避免给注定要删的代码写类型。
- **影响**：全盘路线。
- **日期**：2026-09-18
- **状态**：✅ 已定

---

## D2. 目录组织：按领域 vs 按技术层

- **背景**：现状按 Electron 的技术层分（`src/main` / `src/preload`），导致功能混淆。
- **备选**：
  - (a) 保留 main/preload，内部细分
  - (b) 改为按业务领域分（app / session / bridge / overlay / tools / providers）
- **结论**：**(b) 按领域分**。
- **理由**：
  1. 现状混淆的根因就是「按技术层分」——`preload/dom/` 里既有桥接逻辑又有 UI。
  2. 领域划分让「一个模块一件事」可落地。
  3. Electron 的 main/preload 边界**已由 `main.js`/`preload.js` 薄壳保留**，不影响打包约定。
- **影响**：全盘目录结构（见 `01-architecture.md`）。
- **日期**：2026-09-18
- **状态**：✅ 已定

---

## D3. `cuckoo-tools.d.ts` 契约可否修改？ + 契约演进策略

- **背景**：该文件是给 AI 的 JS API 契约（提示词的一部分）。涉及能否给 `GlobToolNew` 改名、能否删旧别名。
  AI 无状态，但**会话有状态**：系统提示词在 initProject 时发一次，老会话的 AI 上下文里仍是老版本约定。
  问题窗口 = 「**老会话** × **系统升级**」。
- **备选**：
  - (a) 冻结，永不改
  - (b) 可改，但每次改动记录
  - (c) 可改，无约束
- **结论**：**(b) 可改，但每次改动记录**。遵循下述**演进策略**。

### 契约演进策略

**A. 不做运行时兼容（核心决定）**
- 老的 API 名**直接从沙箱删除** —— `JsRunner` 不再注入
  `readFile`/`writeFile`/`editFile`/`readFileWithLines`。
- AI 若写老名字，JS 引擎天然报 `ReferenceError`，**无需任何检测逻辑**
  （沙箱注入什么，AI 就只能用什么）。
- **不维护"自动转换执行"的适配表**（那样老代码能跑 = 永久兼容 = 技术债）。

**B. 报错时给明确指引（错误映射表）**
- 维护一张**极小**的映射表，**仅用于报错文案，不用于执行**：
  ```
  readFile          → read
  readFileWithLines → readLines
  writeFile         → write
  editFile          → edit
  ```
- 命中时返回："`readFile` 已废弃，请改用 `read(path, options)`：
  file_path（必填）、offset（可选）、limit（可选）"
- 此表**只改报错文案**，绝不转换执行 —— 因此**不是**被否决的"适配表"。

**C. 设计上避免破坏性变更（最重要）**
1. 新功能 = **新工具名**，不改老工具。
2. 已有工具加参数 = **可选参数**（`test(a,b)` → `test(a,b,c?)`）。
3. 改语义 = **绝不**，宁可新增工具。

守住 C → 破坏性变更极少。

### 提示词版本号

- **形式**：**手动常量**（如 `PROMPT_VERSION = 5`），改提示词/工具规范时**人工 +1**。
- **与项目版本号区分**：提示词版本 ≠ `package.json` 版本。
  改 UI / 修 bug 不动提示词版本；改工具定义或提示词内容才动。
- **纪律保障**：**在 release 发布文档里写明"本次是否变更了提示词版本"**。
  用户习惯让 AI 阅读 release 文档 → AI 会提醒"提示词版本已变更，需 +1"，
  弥补手动常量可能遗漏的风险。

### 契约改动纪律

**契约改动 = 三处同步 + 记录**：
1. `cuckoo-tools.d.ts`（契约文件）
2. 提示词模板（`src/prompt/*.md` 里的 `{{TOOL_API_TYPES}}` 占位来源）
3. `JsRunner` 注入的函数

三处一起改、一起测，并记入本文件的决策记录。

> 注：P5 之后，第 1、2 处将由工具类型**自动生成**（见 D12），届时只需改工具本身。

- **影响**：命名统一（`New` 后缀）、旧别名删除时机。
- **日期**：2026-09-18
- **状态**：✅ 已定

---

## D4. 旧 JS API 别名（`readFile`/`writeFile`/`editFile`）是否保留？

- **背景**：`JsRunner` 同时注入新旧两套 API。旧的（`readFile`/`writeFile`/`editFile`/`readFileWithLines`）在 `cuckoo-tools.d.ts` 里**没有**。
- **备选**：
  - (a) 保留到 P5，标为兼容层
  - (b) 直接删（不做兼容）
- **结论**：**(b) 直接删，不做兼容**。
  - 沙箱不再注入旧名 → AI 写老名字天然 `ReferenceError`。
  - 报错时用映射表给指引（见 D3-B），**但不转换执行**。
  - `FileReadTool`/`FileWriteTool`/`FileEditTool` 随之删除。
- **理由**：用户明确"不做兼容，默认老会话已不再使用"。
- **影响**：P1 可删三个旧工具类 + 对应测试。
- **日期**：2026-09-18
- **状态**：✅ 已定

---

## D5. 提示词（prompt）如何组织？

- **背景**：现状 `src/prompt/` 下四份 md（deepseek/claude/chatgpt/default）。
  曾以为可合并，**核对后推翻**：
  - `deepseek` vs `default`：仅差 1 处（```ts` 后一个空格）→ 实质相同
  - `claude` vs `chatgpt`：**完全相同**
  - 但 deepseek 组 与 claude 组 **风格完全不同**：deepseek 是专业 agent 提示词；
    claude/chatgpt 是**绕道引导**版（某些模型限制专业 agent 提示词，需改用叙述式引导）
- **澄清（用户）**：
  1. `default.md` 是**自定义 provider 的兜底** —— 用户不写提示词时用它。
  2. 不是"小白版 vs 专业版"，是**不同 AI 网页版对 agent 提示词的接受度不同**，
     故各 provider 需**独立可改的提示词**。
  3. **每个 provider 都应有自己的提示词，不合并。**
  4. **`deepseek`/`claude`/`chatgpt` 本质上就是 provider**（内置，不需用户导入），
     与自定义 provider **同等地位**。
- **结论**：**提示词可选，两种形式并存**（机制已存在，无需改动）：
  - **provider 可实现 `getPromptTemplate()`** → 提示词内联进 provider。
  - **不实现则读文件** `src/prompt/{providerId}.md` → 文件形式。
  - 加载优先级：`provider.getPromptTemplate()` → `src/prompt/{id}.md` → `src/prompt/default.md`。
  - 现状即此逻辑（`project-context.js` L116-131），**内置 provider 用文件形式**。
  - `default.md` 保留为**自定义 provider 的兜底**。
- **影响**：`src/prompt/` **保留**（内置 provider 的提示词来源 + 兜底），不删除、不搬家。
- **日期**：2026-09-18
- **状态**：✅ 已定

---

## D6. IPC 通道是否重命名？

- **背景**：现状 kebab-case（`init-project`、`execute-tool`）。
- **结论**：**不重命名**。
- **理由**：改动面大（主进程 + preload + 测试），收益低。
- **影响**：无。
- **日期**：2026-09-18
- **状态**：✅ 已定

---

## D7. `tools/` 是否移入 `src/`？

- **背景**：现状 `tools/` 在根目录，与 `src/` 分离。
- **备选**：
  - (a) 保留在根
  - (b) 移入 `src/tools/`
- **结论**：**(b) 移入 src**（P4 执行）。
- **代价**：需同步改 `package.json` 的 `extraResources`、electron-builder 配置、测试路径。
- **影响**：tsconfig 简化（单一 `src` 根）。
- **日期**：2026-09-18
- **状态**：✅ 已定（执行时机 P4）

---

## D8. 模块间依赖规则如何强制？

- **背景**：`01-architecture.md` 定义了单向依赖，但需机制保证。
- **备选**：
  - (a) 只写文档，靠自觉
  - (b) ESLint `import/no-restricted-paths`
  - (c) 自定义脚本检查
- **结论**：**(b) ESLint 规则**（P2 引入）。
- **理由**：IDE 实时提示、CI 可拦、配置简单。
- **影响**：P2 需引入 ESLint。
- **日期**：2026-09-18
- **状态**：✅ 已定

---

## D9. TS 严格度：一步到位 vs 渐进？

- **背景**：13,500 行 JS 迁 TS。
- **备选**：
  - (a) `strict: true` 从一开始
  - (b) `strict: false` + `allowJs`，P5 再收紧
- **结论**：**(b) 渐进**。P2 用 `allowJs + checkJs: false`，P3 迁移时允许 `any`，P5 开 `strict`。
- **理由**：一步到位会导致海量报错，阻塞进度。
- **影响**：P5 需专门一轮「类型收紧」。
- **日期**：2026-09-18
- **状态**：✅ 已定

---

## D10. 重构期间 master 是否冻结？

- **背景**：重构耗时长，期间可能有新功能需求。
- **备选**：
  - (a) 冻结 master，重构完成再开
  - (b) 正常开发，重构分支定期合并 master
- **结论**：**(b) 不冻结**。
  - 理由（用户）：**个人独立开发**，无需用冻结来协调多人，灵活优先。
  - 重构分支定期从 master 合并，冲突就地解决。
- **日期**：2026-09-18
- **状态**：✅ 已定

---

## D11. 两种调用模式（JSON / JS）如何处理？

- **背景**：现状**同时支持两种调用模式**，各有一套工具名，共 4 条路径：
  ```
  JSON 模式：{"toolName":"file_read"} → registry 'file_read'
             {"toolName":"read"}      → registry 'read'
  JS 模式：  await readFile(...)       → 沙箱 readFile
             await read(...)          → 沙箱 read
  ```
  这解释了"两套工具并存"的真正原因 —— 不是新旧版本，是**两种模式的配套名字**。
- **备选**：
  - (a) 都保留
  - (b) 只留 JS 模式（JSON 模式废弃）
- **结论**：**(b) 只留 JS 模式**。
  - JSON 模式能力天花板低（无法循环、无法组合、无法把结果喂给下一个工具），
    且 `JsRunner` 沙箱只有 JS 模式用得上。
  - **JSON 模式的挽救**：检测到 AI 用 JSON 调用时，**只发"工具规范已更新"章节**
    （**不发完整初始化提示词** —— 避免重置项目目录、MCP 状态，打断工作）。
  - 只发工具规范 TS 章节即可。
- **影响**：删 `tool-parser`（JSON 解析）、`handleToolCall` 分支；工具只留 `read` 一套名字。
- **执行时机**：P4（需充分测试，涉及 AI 行为）。
- **日期**：2026-09-18
- **状态**：✅ 已定

---

## D12. 工具规范（TS 契约）如何维护？

- **背景**：现状**三处重复**维护同一件事：
  1. `tools/cuckoo-tools.d.ts`（手写完整 TS 契约）
  2. 工具的 `jsApi` 字段（手写字符串签名）
  3. `JsRunner` 的 BOOTSTRAP（手写沙箱注入）
  用户诉求：**不要单独维护这个 TS 文件**，希望"加载多少工具，就自动根据工具本身的
  TS 接口生成规范，暴露给提示词"。
- **备选**：
  - (a) 工具代码里写结构化对象，运行时生成
  - (b) 每个工具旁放 `.d.ts` 片段，构建脚本合并
  - (c) 工具是 TS 类，用 TS 编译器提取类型
- **结论**：**(c) 用 TS 编译器提取**，**放到 P5 执行**。
  - 每个工具实现统一接口，构建期从类型定义**自动生成** TS 规范。
  - 工具本身成为**唯一来源** —— 改工具即改规范，另两处自动同步。
- **理由**：最自动、最准确（真实类型）；但依赖 TS 构建链，与"先重构再 TS"的
  节奏冲突，故排到 P5（此时全部代码已迁 TS）。
- **过渡**：P1–P4 期间，三处仍需手动同步（D3 的"契约改动纪律"约束）。
- **影响**：P5 后 D3 的"三处同步"降为"改工具本身"一处。
- **日期**：2026-09-18
- **状态**：✅ 已定（执行时机 P5）

---

## D13. provider 的文件组织？

- **背景**：provider 是否自包含、提示词放哪。
- **备选**：
  - (a) 全内联进 provider 单文件（含提示词）
  - (b) 元数据 + hook 内联，提示词外置文件
  - (c) 内置用目录，自定义单文件
- **结论**：**(b) provider 单文件（元数据 + hook），提示词外置文件**。
  - 一个 provider 文件 = 元数据 + 内联 hook 源码，**自包含**。
  - **提示词可选**：可实现 `getPromptTemplate()` 内联，也可外置为
    `src/prompt/{id}.md`（加载机制已存在，见 D5）。**内置用文件形式。**
  - **内置 provider（deepseek/claude/chatgpt）与自定义 provider 结构一致** ——
    只是内置的随程序发布、不需用户导入。
- **影响**：`providers/shared/` 仅内置 provider 可引用；
  自定义 provider 不得依赖外部（见 D14）。
- **日期**：2026-09-18
- **状态**：✅ 已定

---

## D14. provider 是否强制实现 TS 接口？

- **背景**：希望内置 + 自定义 provider 都实现统一接口，标准化。
- **结论**：**是，统一实现 `Provider` 接口**。
  - 接口定义提升到 `providers/types.ts`（原 `custom/provider.d.ts`）。
  - **内置与自定义用同一接口**，无区别。
- **⚠️ 技术现实（必须知道）**：
  - **TS 类型在运行时被擦除** —— 编译后无法靠类型"强制"。
  - 故"必须实现接口"最终靠**运行时字段校验函数**
    （检查 `id`/`name`/`homeUrl` 等必填，缺失则报错）。
  - TS 接口提供**写代码时**的 IDE 提示 + 编译期检查；运行时校验提供**真正强制**。
  - 两者**都要有**：类型给开发体验，校验给运行保障。
- **日期**：2026-09-18
- **状态**：✅ 已定

---

## D15. 自定义 provider 上传什么格式？

- **背景**：TS 源码必须编译成 JS 才能跑；用户上传的源码系统必然要编译。
- **备选**：
  - (a) 上传 `.js`（用户自行编译或直接写 JS）
  - (b) 上传 `.ts`，系统引入运行时编译器
  - (c) 两者都收
- **结论**：**(a) 上传 `.js`**。
  - **用户 provider 这一侧**不引入运行时编译器（指编译"用户上传的文件"）。
  - 用户上传**可直接运行的 JS**；写 TS 的人自行 `tsc` 后上传。
  - ⚠️ 与 D16 不矛盾：D16 的 tsx 是**项目自身开发**用；此处说的是
    **用户上传的 provider 文件**不由系统编译。
  - 理由（用户）："这个文件一般是 AI 生成的" —— AI 写 JS 无门槛。
  - **接受破坏性升级**：不兼容已写的 JS provider（本次升级允许）。
- **影响**：内置 provider 由项目构建时编译为 JS 发布；用户 provider 上传 JS。
  **运行时看两者完全统一**（都加载 JS + 校验同一接口）。
- **日期**：2026-09-18
- **状态**：✅ 已定

---

## D16. TS 如何运行（开发 / 发布）？

- **背景**：项目零构建（`electron .` 直接跑 JS），但 Node/Electron 不能直接跑 .ts。
  P3「改名为 .ts」必须有配套机制。参考上游 dsh 的做法。
- **调研**：
  - dsh：开发用 `node --import tsx`，发布用 `tsc` 编译成 `lib/*.js`。
  - Electron 44 内置 Node 24（type stripping 已稳定），但**仅支持可擦除语法**，
    enum / namespace / 参数属性等不支持 → **有语法局限**。
- **备选**：
  - (a) 纯 tsx 运行时（开发+打包都用）
  - (b) tsx 开发 + tsc 编译发布（dsh 模式）
  - (c) Electron 44 原生 strip-types
- **结论**：**(b) tsx 开发 + tsc 编译发布**（dsh 模式）。
  - **开发**：`tsx` 运行时转译，源码即运行，改完直接跑。
  - **发布**：`tsc`（或 esbuild）编译成 JS，产物**不带 tsx 运行时**。
  - 理由（用户）：要"趟通后续开发的路、消除局限" → 不接受 (c) 的语法限制。
- **影响**：P3 需配 tsx 启动；打包流程需加编译步骤。
- **探路验证（2026-09-19）**：
  - Electron 主进程顶层 `require('tsx/cjs')` 后，可加载 `.ts`。✅
  - **无扩展名 `require('./x')` 自动解析到 `x.ts`** → **迁移只改名，引用点不动**。✅
  - 机制：`main.js`（薄壳）顶部注册 tsx/cjs 钩子即可。
- **日期**：2026-09-19
- **状态**：✅ 已定

---

## D17. 依赖升级策略？

- **背景**：用户希望借本次重构把依赖（含 Node/Electron）升到最新稳定版。
- **结论**：**本次重构期间升级到最新稳定版**。
  - Electron 33.4.11 → 44.x（Node 20 → 24）
  - 其余依赖按最新稳定版升级
  - **接受破坏性升级**（与 D4/D15 一致）
- **执行顺序**：**先升级依赖（P2.5），再迁 TS（P3）**（D54）。
  - 升级作为独立阶段，验证通过后进 P3。
  - 理由：tsx 在 Electron 44（Node 24）上最顺；升级与 TS 迁移分离，符合 D1。
- **影响**：需专项验证（Electron 跨 11 个大版本，API 可能有变）。
  作为独立阶段（P2.5）执行，不与 TS 迁移混淆。
- **日期**：2026-09-19
- **状态**：✅ 已定

---

## 汇总

| 编号 | 决策 | 状态 |
|---|---|---|
| D1 | 先 TS 后重构 | ✅ |
| D2 | 按领域分目录 | ✅ |
| D3 | 契约可改 + 演进策略 | ✅ |
| D4 | 旧别名直接删（不兼容） | ✅ |
| D5 | 提示词归属 provider（不合并） | ✅ |
| D6 | IPC 不重命名 | ✅ |
| D7 | tools 移入 src | ✅ |
| D8 | ESLint 强制依赖 | ✅ |
| D9 | TS 渐进 | ✅ |
| D10 | 不冻结 master | ✅ |
| D11 | 只留 JS 模式 | ✅ |
| D12 | 工具规范自动生成（P5） | ✅ |
| D13 | provider 单文件（提示词可外置） | ✅ |
| D14 | provider 强制实现 TS 接口 | ✅ |
| D15 | 自定义 provider 上传 .js | ✅ |
| D16 | TS 运行：tsx 开发 + tsc 发布 | ✅ |
| D17 | 依赖升级到最新稳定版 | ✅ |
